{
  "name": "applyflow-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"vercel dev --listen 3001\"",
    "build": "cd client && npm install && npm run build",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "googleapis": "^140.0.0",
    "openai": "^4.24.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "vercel": "^37.0.0"
  }
}
